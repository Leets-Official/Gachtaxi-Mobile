import 'package:flutter/material.dart';

class ManualMatchingScreen extends StatelessWidget {
  const ManualMatchingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('수동매칭'),
    );
  }
}
