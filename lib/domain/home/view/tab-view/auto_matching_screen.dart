import 'package:flutter/material.dart';

class AutoMatchingScreen extends StatelessWidget {
  const AutoMatchingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('자동매칭'),
    );
  }
}
